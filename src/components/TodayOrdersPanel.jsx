import { useState } from "react";
const money = (value) => `$${Number(value || 0).toLocaleString("zh-TW")}`;
export function TodayOrdersPanel({ orders, onCancelOrder }) {
  const [openId,setOpenId]=useState(null);
  return <aside className="flex min-h-0 flex-col border-l border-white/10 bg-[#0D0F13]"><header className="border-b border-white/10 p-5"><p className="text-sm text-[#C6A96B]/75">TODAY</p><div className="mt-1 flex items-end justify-between"><h1 className="text-2xl font-medium">今日訂單</h1><span className="text-sm text-zinc-400">{orders.length} 筆</span></div></header><div className="min-h-0 flex-1 overflow-y-auto p-4">{orders.length?<div className="space-y-3">{orders.map((sale)=>{const cancelled=sale.status==="cancelled";return <article key={sale.id} className={`rounded-2xl border p-4 ${cancelled?"border-red-400/20 bg-red-500/5":"border-white/10 bg-[#1A1D23]"}`}><button type="button" onClick={()=>setOpenId(openId===sale.id?null:sale.id)} className="w-full text-left"><div className="flex justify-between"><span className="font-medium">{sale.time?.slice(0,5)}</span><span className={cancelled?"text-zinc-500 line-through":"font-semibold text-[#C6A96B]"}>{money(sale.total)}</span></div><p className="mt-2 text-xs text-zinc-500">{sale.mode==="delivery"||sale.mode==="wholesale"?"外送":"現場"} · {sale.paymentMethod==="bank"?"匯款":"現金"} · {cancelled?"已取消":"已完成"}</p><div className="mt-2 space-y-1">{sale.items?.map((item)=><div key={item.id}><p className="truncate text-sm text-zinc-200">{item.name}{item.qty>1?` x ${item.qty}`:""}</p>{item.sauce?<p className="text-xs text-[#C6A96B]">{item.sauce.name}</p>:null}</div>)}</div></button>{openId===sale.id&&!cancelled?<button type="button" onClick={()=>onCancelOrder(sale)} className="mt-4 min-h-12 w-full rounded-xl border border-red-400/25 text-sm font-semibold text-red-300">取消訂單</button>:null}</article>})}</div>:<div className="flex h-full items-center justify-center text-zinc-600">今日尚無訂單</div>}</div></aside>;
}
