import { CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
const money = (value) => `$${Number(value || 0).toLocaleString("zh-TW")}`;
export function DailyRevenueChart({ data }) { return <article className="rounded-2xl border border-white/10 bg-[#1A1D23] p-6 shadow-lg"><p className="text-sm tracking-[0.2em] text-[#C6A96B]/75">DAILY REVENUE</p><h2 className="mt-2 text-2xl font-medium">每日營收趨勢</h2><div className="mt-5 h-80">{data.length?<ResponsiveContainer><LineChart data={data}><CartesianGrid stroke="rgba(198,169,107,.1)" vertical={false}/><XAxis dataKey="date" stroke="#a1a1aa"/><YAxis stroke="#a1a1aa" tickFormatter={money}/><Tooltip formatter={(value)=>[money(value),"營收"]} contentStyle={{background:"#111217",border:"1px solid rgba(198,169,107,.25)",borderRadius:12}}/><Line type="monotone" dataKey="revenue" stroke="#C6A96B" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer>:<div className="flex h-full items-center justify-center text-zinc-500">尚無營收資料</div>}</div></article>; }
