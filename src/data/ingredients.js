import {
  flavorOptions,
  inventoryIngredients,
  inventoryMapping
} from "../config/inventoryMapping.js";

export const ingredients = inventoryIngredients;
export const sauceOptions = flavorOptions;
export const productInventoryUsage = inventoryMapping;
